<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.JoineeTypesActivity"
    android:padding="@dimen/spacing_large">

    <!-- Donor type layout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_donor_type"
        android:layout_width="0dp"
        android:layout_height="100dp"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="@dimen/spacing_normal"
        android:background="@drawable/donor_bg">

        <TextView
            android:id="@+id/tv_donor"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginStart="@dimen/spacing_huge"
            android:gravity="center_vertical"
            android:text="@string/donor_type_label"
            android:textColor="@android:color/white"
            android:textSize="@dimen/font_xxlarge"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/layout_donor_type"
            app:layout_constraintStart_toStartOf="@id/layout_donor_type"
            app:layout_constraintTop_toTopOf="@id/layout_donor_type" />

        <ImageView
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:src="@drawable/donate"
            app:layout_constraintEnd_toEndOf="@id/layout_donor_type"
            app:layout_constraintTop_toTopOf="@id/layout_donor_type"
            android:layout_marginEnd="@dimen/spacing_huge"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- NGO/group layout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_ngo_or_group_type"
        android:layout_width="0dp"
        android:layout_height="100dp"
        app:layout_constraintTop_toBottomOf="@+id/layout_donor_type"
        app:layout_constraintStart_toStartOf="@+id/layout_donor_type"
        app:layout_constraintEnd_toEndOf="@+id/layout_donor_type"
        android:layout_marginTop="@dimen/spacing_huge"
        android:background="@drawable/ngo_group_bg">

        <TextView
            android:id="@+id/tv_ngo_or_group"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginStart="@dimen/spacing_huge"
            android:gravity="center_vertical"
            android:textColor="@android:color/black"
            android:textSize="@dimen/font_xxlarge"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/layout_ngo_or_group_type"
            app:layout_constraintStart_toStartOf="@id/layout_ngo_or_group_type"
            app:layout_constraintTop_toTopOf="@id/layout_ngo_or_group_type"
            android:text="@string/ngo_or_group_label" />

        <ImageView
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:src="@drawable/caring"
            app:layout_constraintEnd_toEndOf="@id/layout_ngo_or_group_type"
            app:layout_constraintTop_toTopOf="@id/layout_ngo_or_group_type"
            android:layout_marginEnd="@dimen/spacing_huge"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Volunteer member layout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_volunteer_type"
        android:layout_width="0dp"
        android:layout_height="100dp"
        app:layout_constraintTop_toBottomOf="@+id/layout_ngo_or_group_type"
        app:layout_constraintStart_toStartOf="@+id/layout_donor_type"
        app:layout_constraintEnd_toEndOf="@+id/layout_donor_type"
        android:layout_marginTop="@dimen/spacing_huge"
        android:background="@drawable/volunteer_bg">

        <TextView
            android:id="@+id/tv_volunteer"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginStart="@dimen/spacing_huge"
            android:gravity="center_vertical"
            android:text="@string/tv_volunteer_label"
            android:textColor="@android:color/white"
            android:textSize="@dimen/font_xxlarge"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/layout_volunteer_type"
            app:layout_constraintStart_toStartOf="@id/layout_volunteer_type"
            app:layout_constraintTop_toTopOf="@id/layout_volunteer_type" />

        <ImageView
            android:layout_width="90dp"
            android:layout_height="wrap_content"
            android:src="@drawable/volunteer"
            app:layout_constraintEnd_toEndOf="@id/layout_volunteer_type"
            app:layout_constraintTop_toTopOf="@id/layout_volunteer_type"
            android:layout_marginEnd="@dimen/spacing_huge"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.constraintlayout.widget.ConstraintLayout>